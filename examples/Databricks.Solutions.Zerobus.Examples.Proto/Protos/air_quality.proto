syntax = "proto2";

package zerobus.example;

option csharp_namespace = "Zerobus.Example.Proto";

// Generate this from your Unity Catalog table so the fields match the table schema exactly.
message AirQuality {
  optional string device_name = 1;
  optional int32 temp = 2;
  optional int64 humidity = 3;
}
